apiVersion: v1
kind: Service
metadata:
  name: lazy-gpt-service
spec:
  type: NodePort
  selector:
    app: lazy-gpt-app
  ports:
    - name: lazy-gpt-port
      protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30007